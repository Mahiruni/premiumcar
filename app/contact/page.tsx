import Link from 'next/link';
import { MessageCircle, ShieldAlert, LockKeyhole } from 'lucide-react';
import { Header, Footer } from '@/components/Marketplace';

export default function ContactPage() {
  return <><Header/><main className="page"><div className="container" style={{maxWidth:860}}><div className="small">SUPPORT</div><h1 style={{fontSize:48,letterSpacing:'-2px',margin:'8px 0 12px'}}>Contact us</h1><p className="small" style={{fontSize:15,lineHeight:1.8}}>Need help with an account, listing, report or privacy request? Start with the marketplace assistant for instant guidance, then use the relevant account or listing controls when available.</p><div className="grid" style={{marginTop:28}}><Link href="/help" className="panel" style={{textDecoration:'none'}}><MessageCircle size={22} style={{color:'var(--accent)',marginBottom:16}}/><h2>Open Help Centre</h2><p className="small">Find answers about buying, selling, accounts, safety, privacy and terms.</p><span className="small" style={{color:'var(--accent)'}}>Get help →</span></Link><Link href="/safety" className="panel" style={{textDecoration:'none'}}><ShieldAlert size={22} style={{color:'var(--accent)',marginBottom:16}}/><h2>Report a safety concern</h2><p className="small">If a transaction looks suspicious, stop and review the Safety Centre guidance before proceeding.</p><span className="small" style={{color:'var(--accent)'}}>Safety Centre →</span></Link><Link href="/privacy" className="panel" style={{textDecoration:'none'}}><LockKeyhole size={22} style={{color:'var(--accent)',marginBottom:16}}/><h2>Privacy request</h2><p className="small">Read how marketplace information is handled and what choices are available to you.</p><span className="small" style={{color:'var(--accent)'}}>Privacy Policy →</span></Link></div><div className="panel" style={{marginTop:16}}><h2>For account or listing problems</h2><p>If you are signed in, check your dashboard first. For a specific listing, use its available actions and keep the listing ID or URL when describing the problem. Never send your password, one-time code, card number or other secret credentials to anyone claiming to be support.</p><p className="small" style={{marginBottom:0}}>The chat assistant is available from the bottom-right corner of every page.</p></div></div></main><Footer/></>;
}
