'use client';
import {useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import {Search,Heart,Plus,MapPin,ShieldCheck,ChevronRight,MessageCircle,SlidersHorizontal,UserRound,LayoutDashboard,LogOut} from 'lucide-react';
import {supabase} from '@/lib/supabase';
import {categories,cities,listings as demoListings,money} from '@/lib/data';

const fallback=demoListings;

function useSession(){const [session,setSession]=useState<any>(null);useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>setSession(data.session));const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);return session}
function imageFor(row:any){const path=row.listing_images?.[0]?.storage_path;if(path&&supabase){return supabase.storage.from('listing-images').getPublicUrl(path).data.publicUrl}return row.image||fallback.find(x=>x.id===row.id)?.image||fallback[0].image}
function normalize(row:any){return {id:row.id,title:row.title,price:Number(row.price),city:row.city,category:row.categories?.name||row.category||'Marketplace',condition:row.condition||'Used',time:new Date(row.created_at).toLocaleDateString(),verified:!!row.verified,description:row.description,image:imageFor(row),seller_id:row.seller_id}}

export function Header(){const [open,setOpen]=useState(false);const session=useSession();useEffect(()=>{const k=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',k);document.body.style.overflow=open?'hidden':'';return()=>{window.removeEventListener('keydown',k);document.body.style.overflow=''}},[open]);const close=()=>setOpen(false);return <><div className="top"><div className="container topin"><span>🇪🇹 Ethiopia's local marketplace</span><span className="topnote">Safe buying starts with meeting in public places.</span></div></div><header className="nav"><div className="container navin"><Link href="/" className="brand" onClick={close}><span className="mark">H</span><span>Habesha Market</span></Link><nav className="navlinks"><Link href="/search">Browse</Link><Link href="/search?category=Cars">Cars</Link><Link href="/search?category=Real%20estate">Real estate</Link><Link href="/search?category=Electronics">Electronics</Link><Link href="/search?category=Jobs">Jobs</Link></nav><div className="actions">{session?<Link href="/dashboard" className="ghost accountlink"><UserRound size={15}/> My account</Link>:<Link href="/login" className="ghost accountlink"><UserRound size={15}/> Sign in</Link>}<Link href="/sell" className="primary selltop"><Plus size={16}/> Sell</Link><button className={`mobilemenu ${open?'is-open':''}`} onClick={()=>setOpen(!open)} aria-label={open?'Close navigation':'Open navigation'} aria-expanded={open}><span className="hamburger-lines"><span/><span/><span/></span></button></div></div></header>{open&&<><button className="menuoverlay" aria-label="Close menu" onClick={close}/><div className="mobilepanel" id="mobile-navigation" role="dialog"><div className="mobilepanel-inner"><div className="mobile-account"><div className="user-avatar"><UserRound size={21}/></div><div><strong>{session?'Your marketplace':'Welcome to Habesha Market'}</strong><span>{session?.user?.email||'Sign in to save ads and message sellers'}</span></div></div>{session?<><Link className="mobile-account-cta" href="/dashboard" onClick={close}><LayoutDashboard size={17}/> My dashboard <ChevronRight size={16}/></Link><Link className="mobile-account-cta" href="/search?saved=1" onClick={close} style={{marginTop:8}}><Heart size={17}/> Saved listings <ChevronRight size={16}/></Link></>:<Link className="mobile-account-cta" href="/login" onClick={close}><UserRound size={17}/> Sign in / Create account <ChevronRight size={16}/></Link>}<div className="mobile-search"><Search size={17}/><Link href="/search" onClick={close}>Search cars, homes, phones & more</Link></div><div className="mobile-menu-section"><div className="mobile-label">BROWSE</div>{[['🚗','Cars'],['🏠','Real estate'],['📱','Electronics'],['💼','Jobs']].map(([i,n])=><Link key={n} href={`/search?category=${encodeURIComponent(n)}`} onClick={close}>{i} {n} <ChevronRight size={16}/></Link>)}</div><Link className="mobile-sell" href="/sell" onClick={close}><Plus size={18}/> Post an ad</Link><p className="mobile-safety">🇪🇹 Built for Ethiopia · Buy locally, meet safely.</p></div></div></>}</>}

export function ListingCard({item}:{item:any}){const session=useSession();const [fav,setFav]=useState(false);useEffect(()=>{if(!supabase||!session)return;supabase.from('favorites').select('listing_id').eq('user_id',session.user.id).eq('listing_id',item.id).maybeSingle().then(({data})=>setFav(!!data))},[session,item.id]);async function toggle(e:any){e.preventDefault();if(!supabase||!session){window.location.href='/login';return}if(fav){await supabase.from('favorites').delete().eq('user_id',session.user.id).eq('listing_id',item.id);setFav(false)}else{const {error}=await supabase.from('favorites').insert({user_id:session.user.id,listing_id:item.id});if(!error)setFav(true)}}return <Link href={`/listing/${item.id}`} className="card"><div className="photo"><img src={item.image} alt={item.title}/>{item.verified&&<span className="badge">VERIFIED</span>}<button className="heart" onClick={toggle} aria-label={fav?'Remove from favorites':'Save listing'}>{fav?'♥':'♡'}</button></div><div className="cardbody"><div className="price">{money(item.price)}</div><div className="title">{item.title}</div><div className="meta"><MapPin size={12}/>{item.city} · {item.time}</div>{item.verified&&<div className="trust"><ShieldCheck size={12}/> Verified seller</div>}</div></Link>}

export function Footer(){return <footer className="footer"><div className="container footergrid"><div><div className="brand"><span className="mark">H</span> Habesha Market</div><p>Buy locally. Sell confidently. Built for Ethiopia.</p></div><div><h4>Marketplace</h4><p>Cars<br/>Homes<br/>Electronics<br/>Jobs</p></div><div><h4>For sellers</h4><p>Post an ad<br/>Seller guide<br/>Safety centre</p></div><div><h4>Help</h4><p>Contact us<br/>Terms<br/>Privacy</p></div></div></footer>}

async function fetchListings(){if(!supabase)return fallback;const {data,error}=await supabase.from('listings').select('*,categories(name),listing_images(storage_path)').eq('status','active').order('created_at',{ascending:false}).limit(40);if(error||!data?.length)return fallback;return data.map(normalize)}

export function Home(){const [items,setItems]=useState<any[]>(fallback);useEffect(()=>{fetchListings().then(setItems)},[]);return <><Header/><main><section className="hero"><div className="container"><div className="small">ADDIS ABABA · ETHIOPIA</div><h1>Find your next thing.<br/><em style={{fontStyle:'normal',color:'var(--accent)'}}>Right here.</em></h1><p>Cars, homes, phones, furniture, jobs and everything in between — from people and businesses across Ethiopia.</p><form className="searchbox" action="/search"><Search size={20}/><input name="q" placeholder="What are you looking for?"/><select className="select" name="city"><option>All Ethiopia</option>{cities.map(c=><option key={c}>{c}</option>)}</select><button className="searchbtn">Search</button></form></div></section><section className="section"><div className="container"><div className="sectionhead"><h2>Browse categories</h2><Link className="small" href="/search">See all <ChevronRight size={13}/></Link></div><div className="categories">{categories.map((c,i)=><Link className="cat" href={`/search?category=${encodeURIComponent(c[3])}`} key={i}><span className="emoji">{c[0]}</span><span>{c[2]}</span></Link>)}</div></div></section><section className="section" style={{paddingTop:0}}><div className="container"><div className="sectionhead"><div><h2>Fresh listings</h2><div className="small">Live listings from the marketplace</div></div><Link className="small" href="/search">View all</Link></div><div className="grid">{items.map(x=><ListingCard item={x} key={x.id}/>)}</div></div></section><section className="section"><div className="container panel" style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:20,background:'#f1e7df'}}><div><div className="small">SELL SOMETHING TODAY</div><h2 style={{margin:'6px 0'}}>Turn unused into useful.</h2><div className="small">Post your first ad in a few minutes.</div></div><Link className="primary" href="/sell"><Plus size={16}/> Post an ad</Link></div></section></main><Footer/></>}

export function SearchPage(){const [items,setItems]=useState<any[]>([]);const [q,setQ]=useState('');const [city,setCity]=useState('All Ethiopia');const [cat,setCat]=useState('All categories');const [max,setMax]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{const p=new URLSearchParams(location.search);setQ(p.get('q')||'');setCat(p.get('category')||'All categories');setCity(p.get('city')||'All Ethiopia');fetchListings().then(x=>{setItems(x);setLoading(false)})},[]);const filtered=useMemo(()=>items.filter(x=>(!q||`${x.title} ${x.description||''}`.toLowerCase().includes(q.toLowerCase()))&&(city==='All Ethiopia'||x.city===city)&&(cat==='All categories'||x.category===cat)&&(!max||x.price<=Number(max))),[items,q,city,cat,max]);return <><Header/><main className="page"><div className="container"><div className="small">MARKETPLACE</div><h1 style={{fontSize:42,letterSpacing:'-2px',margin:'8px 0'}}>All listings</h1><div className="toolbar"><div style={{flex:'1 1 300px',display:'flex',border:'1px solid var(--line)',borderRadius:12,background:'#fff',padding:'0 12px'}}><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search listings" style={{border:0,outline:0,padding:12,background:'transparent',width:'100%'}}/></div><select className="pill" value={cat} onChange={e=>setCat(e.target.value)}><option>All categories</option>{categories.map(c=><option key={c[3]}>{c[3]}</option>)}</select><select className="pill" value={city} onChange={e=>setCity(e.target.value)}><option>All Ethiopia</option>{cities.map(x=><option key={x}>{x}</option>)}</select><input className="pill" value={max} onChange={e=>setMax(e.target.value)} placeholder="Max price" type="number"/></div><div className="small" style={{marginBottom:16}}>{loading?'Loading…':`${filtered.length} results`} · <SlidersHorizontal size={13}/></div><div className="grid">{filtered.map(x=><ListingCard item={x} key={x.id}/>)}</div>{!loading&&!filtered.length&&<div className="panel" style={{textAlign:'center',padding:60}}>No listings match those filters.</div>}</div></main><Footer/></>}

export function SellPage(){const [cats,setCats]=useState<any[]>([]);const [session,setSession]=useState<any>(null);const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');const [files,setFiles]=useState<File[]>([]);useEffect(()=>{supabase?.auth.getSession().then(({data})=>setSession(data.session));supabase?.from('categories').select('id,name').order('name').then(({data})=>setCats(data||[]))},[]);async function submit(e:any){e.preventDefault();setMsg('');if(!supabase){setMsg('Supabase is not configured.');return}const {data:{user}}=await supabase.auth.getUser();if(!user){window.location.href='/login';return}setBusy(true);const f=new FormData(e.currentTarget);const categoryId=String(f.get('category'));const {data:listing,error}=await supabase.from('listings').insert({seller_id:user.id,category_id:categoryId,title:String(f.get('title')),description:String(f.get('description')),price:Number(f.get('price')),city:String(f.get('city')),condition:String(f.get('condition'))}).select().single();if(error||!listing){setMsg(error?.message||'Could not create listing.');setBusy(false);return}for(let i=0;i<files.length;i++){const file=files[i];const path=`${user.id}/${listing.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`;const up=await supabase.storage.from('listing-images').upload(path,file,{contentType:file.type,upsert:false});if(!up.error)await supabase.from('listing_images').insert({listing_id:listing.id,storage_path:path,sort_order:i})}setMsg('Listing submitted for moderation. You can manage it from your dashboard.');e.currentTarget.reset();setFiles([]);setBusy(false)}return <><Header/><main className="page"><div className="container" style={{maxWidth:860}}><div className="small">CREATE LISTING</div><h1 style={{fontSize:42,letterSpacing:'-2px'}}>Post an ad</h1>{!session&&<div className="notice">You need an account to publish a listing.</div>}<div className="panel" style={{marginTop:16}}><form className="form" onSubmit={submit}><label>Title<input required minLength={5} name="title" placeholder="e.g. Toyota Corolla 2019"/></label><label>Category<select required name="category"><option value="">Choose category</option>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Price (ETB)<input required min="0" name="price" type="number"/></label><label>City<select required name="city">{cities.map(x=><option key={x}>{x}</option>)}</select></label><label>Condition<select name="condition"><option>Used</option><option>New</option><option>Like new</option></select></label><label>Description<textarea required minLength={10} name="description" placeholder="Tell buyers what makes this listing worth their time..."/></label><label>Photos<input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>setFiles(Array.from(e.target.files||[]).slice(0,8))}/><span className="small">{files.length} photo(s) selected · up to 8</span></label>{msg&&<div className="notice">{msg}</div>}<button className="primary" disabled={busy} type="submit" style={{justifyContent:'center'}}>{busy?'Publishing…':'Publish listing'}</button></form></div></div></main><Footer/></>}

export function Dashboard(){const session=useSession();const [items,setItems]=useState<any[]>([]);const [saved,setSaved]=useState<any[]>([]);const [messages,setMessages]=useState(0);const [loading,setLoading]=useState(true);async function load(){if(!supabase||!session)return;const {data}=await supabase.from('listings').select('*,categories(name),listing_images(storage_path)').eq('seller_id',session.user.id).order('created_at',{ascending:false});setItems((data||[]).map(normalize));const {data:f}=await supabase.from('favorites').select('listing_id, listings(*,categories(name),listing_images(storage_path))').eq('user_id',session.user.id);setSaved((f||[]).map((x:any)=>x.listings).filter(Boolean).map(normalize));const {count}=await supabase.from('messages').select('*',{count:'exact',head:true}).or(`sender_id.eq.${session.user.id},recipient_id.eq.${session.user.id}`);setMessages(count||0);setLoading(false)}useEffect(()=>{load()},[session]);async function logout(){await supabase?.auth.signOut();window.location.href='/'}if(!session)return <><Header/><main className="page"><div className="container"><div className="panel"><h1>Sign in to your dashboard</h1><Link className="primary" href="/login">Sign in</Link></div></div></main><Footer/></>;return <><Header/><main className="page"><div className="container"><div className="sectionhead"><div><div className="small">ACCOUNT</div><h1 style={{fontSize:42,letterSpacing:'-2px',margin:'6px 0'}}>My dashboard</h1><div className="small">{session.user.email}</div></div><div style={{display:'flex',gap:8}}><Link href="/sell" className="primary"><Plus size={16}/> New listing</Link><button className="ghost" onClick={logout}><LogOut size={15}/> Sign out</button></div></div><div className="grid" style={{gridTemplateColumns:'repeat(3,1fr)'}}><div className="panel"><div className="small">ACTIVE LISTINGS</div><div style={{fontSize:32,fontWeight:800,marginTop:8}}>{items.filter(x=>x.status==='active').length}</div></div><div className="panel"><div className="small">SAVED ADS</div><div style={{fontSize:32,fontWeight:800,marginTop:8}}>{saved.length}</div></div><div className="panel"><div className="small">MESSAGES</div><div style={{fontSize:32,fontWeight:800,marginTop:8}}>{messages}</div></div></div><div className="section"><div className="sectionhead"><h2>Your listings</h2><span className="small">{loading?'Loading…':'Manage your ads'}</span></div><div className="grid">{items.map(x=><ListingCard item={x} key={x.id}/>)}</div>{!loading&&!items.length&&<div className="panel">You have no listings yet. <Link href="/sell" style={{color:'var(--accent)',fontWeight:800}}>Post your first ad →</Link></div>}</div><div className="section"><div className="sectionhead"><h2>Saved listings</h2><span className="small">Your favourites</span></div><div className="grid">{saved.map(x=><ListingCard item={x} key={x.id}/>)}</div></div></div></main><Footer/></>}

export function ListingPage({id}:{id:string}){const [item,setItem]=useState<any>(null);const session=useSession();const [body,setBody]=useState('');const [sent,setSent]=useState('');const [fav,setFav]=useState(false);useEffect(()=>{if(!supabase)return;supabase.from('listings').select('*,categories(name),listing_images(storage_path),profiles:seller_id(full_name,username,phone,city)').eq('id',id).maybeSingle().then(({data})=>setItem(data?normalize(data):fallback.find(x=>x.id===id)))} , [id]);useEffect(()=>{if(!supabase||!session)return;supabase.from('favorites').select('listing_id').eq('user_id',session.user.id).eq('listing_id',id).maybeSingle().then(({data})=>setFav(!!data))},[session,id]);async function save(){if(!supabase||!session){window.location.href='/login';return}if(fav){await supabase.from('favorites').delete().eq('user_id',session.user.id).eq('listing_id',id);setFav(false)}else{const {error}=await supabase.from('favorites').insert({user_id:session.user.id,listing_id:id});if(!error)setFav(true)}}async function message(){if(!supabase||!session){window.location.href='/login';return}if(!body.trim()||!item?.seller_id)return;const {error}=await supabase.from('messages').insert({listing_id:id,sender_id:session.user.id,recipient_id:item.seller_id,body:body.trim()});setSent(error?error.message:'Message sent.');if(!error)setBody('')}if(!item)return <><Header/><main className="page"><div className="container"><div className="panel">Loading listing…</div></div></main><Footer/></>;return <><Header/><main className="page"><div className="container"><Link href="/search" className="back">← Back to listings</Link><div className="detail"><div><div className="gallery"><div className="gallerymain"><img src={item.image} alt={item.title}/></div></div><div className="panel" style={{marginTop:16}}><div className="small">DESCRIPTION</div><p style={{lineHeight:1.8}}>{item.description}</p></div></div><aside className="side"><div className="panel"><div className="small">{String(item.category).toUpperCase()} · {item.city}</div><h1 style={{fontSize:30,letterSpacing:'-1px',lineHeight:1.1}}>{item.title}</h1><div className="price" style={{fontSize:25}}>{money(item.price)}</div><div className="statrow"><span>Condition</span><b>{item.condition}</b></div><div className="statrow"><span>Location</span><b>{item.city}</b></div><button className="primary" style={{width:'100%',marginTop:18}} onClick={()=>document.getElementById('message-box')?.scrollIntoView({behavior:'smooth'})}><MessageCircle size={17}/> Message seller</button><button className="ghost" style={{width:'100%',marginTop:8}} onClick={save}><Heart size={16}/> {fav?'Saved':'Save listing'}</button></div><div className="panel" id="message-box" style={{marginTop:12}}><div className="small">MESSAGE SELLER</div><textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Is this still available?" style={{width:'100%',marginTop:10,minHeight:100,padding:12,border:'1px solid var(--line)',borderRadius:10}}/><button className="primary" style={{width:'100%',marginTop:8}} onClick={message}>Send message</button>{sent&&<div className="notice" style={{marginTop:8}}>{sent}</div>}</div><div className="panel" style={{marginTop:12}}><div className="small">SAFETY</div><p style={{fontSize:12,lineHeight:1.6}}>Never send money before seeing the item. Meet in a public place and verify documents.</p></div></aside></div></div></main><Footer/></>}
